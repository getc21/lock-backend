# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration (Choose one)
# For MongoDB
MONGODB_URI=mongodb://localhost:27017/bellezapp

# For PostgreSQL
# DATABASE_URL=postgresql://username:password@localhost:5432/bellezapp

# JWT Configuration
JWT_SECRET=your-secret-key-change-this-in-production
JWT_EXPIRES_IN=7d

# CORS Configuration
CORS_ORIGIN=http://localhost:5173

# SQLite Migration (if migrating from SQLite)
SQLITE_DB_PATH=../bellezapp/database.db
