# Application
NODE_ENV=production
PORT=3000
API_URL=https://naturalmarkets.net

# Database
MONGODB_URI=mongodb+srv://usuario:contraseña@cluster.mongodb.net/bellezapp

# JWT
JWT_SECRET=cambiar_esto_con_valor_seguro_aleatorio_32_caracteres_minimo
JWT_EXPIRE=24h

# Image Storage (Local)
# Las imágenes se guardan en /uploads del servidor
# No requiere configuración de Cloudinary

# Render (Opcional)
RENDER_EXTERNAL_URL=https://naturalmarkets.net

# Notas:
# - JWT_SECRET: Usar openssl rand -hex 16 para generar valor seguro
# - MONGODB_URI: Reemplazar con tu conexión real (MongoDB Atlas o local)
# - API_URL: Dominio final sin trailing slash
SQLITE_DB_PATH=../bellezapp/database.db
