server {
    server_name api.naturalmarkets.net;

    # Servir archivos estáticos del uploads
    location /uploads {
        alias /var/www/bellezapp-backend/uploads;
        expires 30d;
        add_header Cache-Control "public";
        add_header Access-Control-Allow-Origin "https://naturalmarkets.net";
        add_header Access-Control-Allow-Methods "GET, OPTIONS";
        add_header Access-Control-Allow-Headers "Content-Type, Authorization";
    }

    # Proxy al backend Express
    location / {
        proxy_pass http://127.0.0.1:4000;
        proxy_http_version 1.1;

        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;

        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_cache_bypass $http_upgrade;
    }

    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/api.naturalmarkets.net/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/api.naturalmarkets.net/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot
}

server {
    if ($host = api.naturalmarkets.net) {
        return 301 https://$host$request_uri;
    } # managed by Certbot

    listen 80;
    server_name api.naturalmarkets.net;
    return 404; # managed by Certbot
}
